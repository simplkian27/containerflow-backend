# Environment Variables Example
# Copy this file to .env and fill in your actual values
# Never commit .env to version control

# ============================================================================
# DATABASE CONFIGURATION (REQUIRED)
# ============================================================================
# PostgreSQL connection string for Supabase
# Get this from: Supabase Dashboard → Settings → Database → Connection String (URI format)
# Format: postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
DATABASE_URL=postgresql://postgres:[password]@[host]:[port]/postgres

# ============================================================================
# API DOMAIN CONFIGURATION (REQUIRED)
# ============================================================================
# Public API domain for mobile app to connect to backend
# For local development: localhost:5000
# For Replit: $REPLIT_DEV_DOMAIN:5000
EXPO_PUBLIC_DOMAIN=$REPLIT_DEV_DOMAIN:5000

# ============================================================================
# REPLIT-SPECIFIC CONFIGURATION (Optional - only for Replit deployments)
# ============================================================================
# Replit development domain (set automatically by Replit)
# REPLIT_DEV_DOMAIN=your-app.replit.dev

# Replit internal app domain (set automatically by Replit)
# REPLIT_INTERNAL_APP_DOMAIN=your-app.replit.dev

# ============================================================================
# OPTIONAL: SUPABASE CONFIGURATION
# ============================================================================
# Only needed if you're using Supabase client libraries directly
# SUPABASE_URL=https://[project-ref].supabase.co
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
